import React from 'react';
import TopBar from '../../components/TopBar';
import Header from '../../components/Header';
import Footer from '../../components/Footer';
import { motion } from 'framer-motion';
import { Building2, Users, Award, Briefcase, CheckCircle2, TrendingUp } from 'lucide-react';

const HeroSection = () => (
  <section className="relative h-[60vh] flex items-center">
    <div className="absolute inset-0 bg-gradient-to-br from-[#2E5A9C] to-[#3A3A3C] opacity-95" />
    <div className="absolute inset-0 bg-[url('/images/corporate-hero.jpg')] bg-cover bg-center mix-blend-overlay" />
    <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-white">
      <motion.div
        initial={{ opacity: 0, y: 30 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.8 }}
      >
        <h1 className="text-5xl font-bold mb-6">Espace Entreprises</h1>
        <p className="text-xl max-w-2xl">Partenaire de ton développement RH : formations sur-mesure, recrutement, taxe patronale.</p>
      </motion.div>
    </div>
  </section>
);

const ServicesSection = () => {
  const services = [
    {
      icon: <Users className="w-12 h-12" />,
      title: 'Formations intra-entreprise',
      description: 'Programmes sur-mesure dans vos locaux, adaptés à vos besoins spécifiques',
      benefits: [
        'Contenu personnalisé à votre secteur',
        'Planning flexible selon vos contraintes',
        'Formateurs dédiés à votre entreprise',
        'Tarifs dégressifs dès 8 participants'
      ]
    },
    {
      icon: <Award className="w-12 h-12" />,
      title: 'Plans de formation annuels',
      description: 'Accompagnement dans l\'élaboration et la mise en œuvre de votre plan de formation',
      benefits: [
        'Diagnostic des besoins en compétences',
        'Conception du plan de formation',
        'Suivi et évaluation des impacts',
        'Optimisation de la taxe patronale'
      ]
    },
    {
      icon: <Briefcase className="w-12 h-12" />,
      title: 'Recrutement de profils qualifiés',
      description: 'Accès prioritaire à nos meilleurs étudiants et alumni pour vos recrutements',
      benefits: [
        'CVthèque de 3 500+ alumni',
        'Pré-sélection selon vos critères',
        'Organisation de job dating',
        'Garantie de remplacement 3 mois'
      ]
    },
    {
      icon: <Building2 className="w-12 h-12" />,
      title: 'Stages & alternance',
      description: 'Accueil de stagiaires et alternants pour renforcer vos équipes à moindre coût',
      benefits: [
        'Étudiants BTS, Licence, Master',
        'Encadrement par nos équipes',
        'Conventions tripartites',
        'Possibilité d\'embauche post-stage'
      ]
    }
  ];

  return (
    <section className="py-20 bg-[#F5F1E8]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 className="text-4xl font-bold text-[#3A3A3C] mb-12 text-center">Nos Services aux Entreprises</h2>
        <div className="grid md:grid-cols-2 gap-8">
          {services.map((service, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ delay: index * 0.15 }}
              viewport={{ once: true }}
              className="bg-white p-8 rounded-lg shadow-lg"
            >
              <div className="text-[#2E5A9C] mb-4">{service.icon}</div>
              <h3 className="text-2xl font-bold text-[#3A3A3C] mb-3">{service.title}</h3>
              <p className="text-[#3A3A3C] mb-4">{service.description}</p>
              <ul className="space-y-2">
                {service.benefits.map((benefit, idx) => (
                  <li key={idx} className="flex items-start text-sm">
                    <CheckCircle2 className="w-5 h-5 text-[#2E5A9C] mr-2 mt-0.5 flex-shrink-0" />
                    <span className="text-[#3A3A3C]">{benefit}</span>
                  </li>
                ))}
              </ul>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const ThemesSection = () => {
  const themes = [
    'Leadership & management d\'équipe',
    'Communication interpersonnelle',
    'Gestion de projet',
    'Marketing & vente',
    'Finance pour non-financiers',
    'Gestion des ressources humaines',
    'Excellence opérationnelle',
    'Transformation digitale',
    'Gestion du changement',
    'Qualité & sécurité',
    'Service client',
    'Négociation commerciale'
  ];

  return (
    <section className="py-20 bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 className="text-4xl font-bold text-[#3A3A3C] mb-12 text-center">Thématiques de Formation Entreprise</h2>
        <div className="grid md:grid-cols-3 lg:grid-cols-4 gap-4">
          {themes.map((theme, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, scale: 0.9 }}
              whileInView={{ opacity: 1, scale: 1 }}
              transition={{ delay: index * 0.05 }}
              viewport={{ once: true }}
              className="bg-[#F5F1E8] p-4 rounded-lg text-center hover:bg-[#2E5A9C] hover:text-white transition-colors"
            >
              <CheckCircle2 className="w-5 h-5 mx-auto mb-2" />
              <span className="text-sm font-semibold">{theme}</span>
            </motion.div>
          ))}
        </div>
        <div className="mt-12 text-center">
          <p className="text-[#3A3A3C] mb-4">
            Programme non listé ? Nous concevons des formations 100% sur-mesure selon vos besoins.
          </p>
          <button className="bg-[#2E5A9C] text-white py-3 px-8 rounded-lg hover:bg-[#3A3A3C] transition-colors font-semibold">
            Demander un Programme Sur-Mesure
          </button>
        </div>
      </div>
    </section>
  );
};

const PartnersSection = () => {
  const partners = [
    { name: 'Banque Atlantique', sector: 'Finance', employees: '250 formés/an' },
    { name: 'Sonabel', sector: 'Énergie', employees: '180 formés/an' },
    { name: 'Ouagadougou Hôtel', sector: 'Hôtellerie', employees: '120 formés/an' },
    { name: 'Orange Burkina', sector: 'Télécoms', employees: '200 formés/an' },
    { name: 'Société Générale BF', sector: 'Banque', employees: '150 formés/an' },
    { name: 'Coris Bank', sector: 'Finance', employees: '100 formés/an' }
  ];

  return (
    <section className="py-20 bg-[#F5F1E8]">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 className="text-4xl font-bold text-[#3A3A3C] mb-12 text-center">Ils Nous Font Confiance</h2>
        <div className="grid md:grid-cols-3 gap-6">
          {partners.map((partner, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ delay: index * 0.1 }}
              viewport={{ once: true }}
              className="bg-white p-6 rounded-lg shadow-lg text-center"
            >
              <Building2 className="w-8 h-8 text-[#2E5A9C] mx-auto mb-3" />
              <h3 className="text-lg font-bold text-[#3A3A3C] mb-1">{partner.name}</h3>
              <p className="text-sm text-[#B8956A] mb-2">{partner.sector}</p>
              <p className="text-xs text-[#3A3A3C]">{partner.employees}</p>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const TaxSection = () => (
  <section className="py-20 bg-[#2E5A9C] text-white">
    <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 className="text-4xl font-bold mb-6 text-center">Taxe Patronale de Formation</h2>
      <div className="bg-white/10 backdrop-blur p-8 rounded-lg mb-8">
        <p className="text-xl mb-4">
          RBA est agréé par le Ministère de l'Emploi. Les formations réalisées chez RBA sont déductibles de la taxe patronale.
        </p>
        <ul className="space-y-3">
          <li className="flex items-start">
            <CheckCircle2 className="w-6 h-6 mr-3 flex-shrink-0 mt-1" />
            <span>Récupération jusqu'à 2% de votre masse salariale</span>
          </li>
          <li className="flex items-start">
            <CheckCircle2 className="w-6 h-6 mr-3 flex-shrink-0 mt-1" />
            <span>Attestations fiscales fournies pour chaque formation</span>
          </li>
          <li className="flex items-start">
            <CheckCircle2 className="w-6 h-6 mr-3 flex-shrink-0 mt-1" />
            <span>Accompagnement dans vos démarches administratives</span>
          </li>
        </ul>
      </div>
      <div className="text-center">
        <button className="bg-white text-[#2E5A9C] py-4 px-8 rounded-lg hover:bg-[#F5F1E8] transition-colors font-semibold text-lg">
          Télécharger le Guide Taxe Patronale
        </button>
      </div>
    </div>
  </section>
);

const StatsSection = () => {
  const stats = [
    { value: '450+', label: 'Entreprises clientes' },
    { value: '8 500+', label: 'Salariés formés/an' },
    { value: '92%', label: 'Taux de satisfaction' },
    { value: '150+', label: 'Programmes intra réalisés' }
  ];

  return (
    <section className="py-20 bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div className="grid md:grid-cols-4 gap-8 text-center">
          {stats.map((stat, index) => (
            <motion.div
              key={index}
              initial={{ opacity: 0, scale: 0.8 }}
              whileInView={{ opacity: 1, scale: 1 }}
              transition={{ delay: index * 0.1 }}
              viewport={{ once: true }}
            >
              <TrendingUp className="w-12 h-12 text-[#2E5A9C] mx-auto mb-3" />
              <div className="text-5xl font-bold text-[#2E5A9C] mb-2">{stat.value}</div>
              <div className="text-xl text-[#3A3A3C]">{stat.label}</div>
            </motion.div>
          ))}
        </div>
      </div>
    </section>
  );
};

const CTASection = () => (
  <section className="py-20 bg-[#F5F1E8]">
    <div className="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 className="text-4xl font-bold text-[#3A3A3C] mb-6">Développons Ensemble les Compétences de Vos Équipes</h2>
      <p className="text-xl text-[#3A3A3C] mb-8">
        Contacte notre service entreprises pour un devis personnalisé sous 48h.
      </p>
      <div className="flex flex-col sm:flex-row gap-4 justify-center">
        <button className="bg-[#2E5A9C] text-white py-4 px-8 rounded-lg hover:bg-[#3A3A3C] transition-colors font-semibold text-lg">
          Demander un Devis
        </button>
        <button className="border-2 border-[#2E5A9C] text-[#2E5A9C] py-4 px-8 rounded-lg hover:bg-[#2E5A9C] hover:text-white transition-colors font-semibold text-lg">
          Prendre Rendez-vous
        </button>
      </div>
      <div className="mt-8 text-sm text-[#3A3A3C]">
        <p><strong>Contact dédié entreprises :</strong></p>
        <p>+226 25 30 45 70 | entreprises@rba.bf</p>
      </div>
    </div>
  </section>
);

const Corporate = () => {
  return (
    <div className="min-h-screen bg-white">
      <TopBar />
      <Header />
      <HeroSection />
      <ServicesSection />
      <ThemesSection />
      <PartnersSection />
      <TaxSection />
      <StatsSection />
      <CTASection />
      <Footer />
    </div>
  );
};

export default Corporate;
